// Flox Modules
// eslint-disable-next-line import/prefer-default-export
export enum MODULES {
  AUTH = 'auth',
  ROLES = 'roles',
  FILE = 'file',
  SHARING = 'sharing',
  NOTIFICATION = 'notificaton',
}
