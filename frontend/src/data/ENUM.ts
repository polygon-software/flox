// User roles TODO: Application specific
export enum ROLE {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum FILE_UPLOAD_STATUS {
  READY = 'ready',
  LOADING = 'loading',
  DONE = 'done',
  FAILED = 'failed',
}
