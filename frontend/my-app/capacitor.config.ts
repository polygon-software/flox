import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.polygonsoftware.sample',
  appName: 'Sample App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
